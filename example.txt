#include <iostream>
#include <cmath>

int main() {

setlocale(0, "ru-Ru");

int u, i;
u = 4096;
/*do{
u = sqrt(u);
std::cout << u << "\n";
i -= 1; 
}
while (i >= 0);*/
// hello
for (i = 10; i>=0; i-- ){
    std::cout << i*i << "\n";
}
};;;;